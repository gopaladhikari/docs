import { year } from "../data";

export function Props({ name, year }) {}

# Hello {name.toUpperCase()}

The current year is {year}

# Hello {props.name.toUpperCase()}

The current year is {props.year}
